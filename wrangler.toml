name = "thinknote-blog"
main = "./worker.js"
compatibility_date = "2025-08-01"

[assets]
directory = "./dist"
binding = "ASSETS"
# / must hit the Worker so locale 302 runs before any leftover index.html
run_worker_first = ["/", "/api/*"]
