name = "thinknote-blog"
main = "./worker.js"
compatibility_date = "2025-08-01"

[assets]
directory = "./dist"
binding = "ASSETS"
# Ensure /api/* hits the Worker before static asset lookup
run_worker_first = ["/api/*"]
